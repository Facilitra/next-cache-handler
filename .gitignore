node_modules
*.log
.DS_Store
# pnpm 11 auto-creates this locally with a build-approval placeholder and no
# `packages:` field, which breaks `pnpm store path` in CI. We don't need a
# workspace, so keep it out of the repo.
pnpm-workspace.yaml
